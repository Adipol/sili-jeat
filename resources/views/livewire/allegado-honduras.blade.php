<div>
    @if ($count > 1)
        <a class="btn btn-green" href="{{ route('all.honduras.show', $pep->id_pep) }}">
            <i class="fas far fa-people-arrows"> </i>
        </a>
    @endif
</div>
